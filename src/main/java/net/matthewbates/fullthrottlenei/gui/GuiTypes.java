package net.matthewbates.fullthrottlenei.gui;

// Enums would work better to ensure that we never conflict, but since the networking loses the type information
// its easier to use constants here.
public final class GuiTypes
{
    public static final int EDIT_ELEMENTS = 0;
}
